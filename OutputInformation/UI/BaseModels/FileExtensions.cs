using System.Text.Json;

namespace UI.BaseModels
{
    public class FileExtensions
    {
        public const string docx = "." + nameof(docx);
        public const string html = "." + nameof(html);
        public const string json = "." + nameof(json);
    }
}
